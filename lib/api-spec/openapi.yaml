openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.2.0
  description: Attendance System API with Face Recognition
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: users
    description: User registration operations
  - name: attendance
    description: Attendance tracking operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /register:
    post:
      operationId: registerUser
      tags: [users]
      summary: Register a new user with webcam image and face descriptor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterUserRequest"
      responses:
        "201":
          description: User registered successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterUserResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: User already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users:
    get:
      operationId: getUsers
      tags: [users]
      summary: Get all registered users with attendance statistics
      responses:
        "200":
          description: List of registered users with stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsersListResponse"

  /users/descriptors:
    get:
      operationId: getUserDescriptors
      tags: [users]
      summary: Get all users with their face descriptors for client-side matching
      responses:
        "200":
          description: List of users with face descriptors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDescriptorsResponse"

  /users/{id}:
    delete:
      operationId: deleteUser
      tags: [users]
      summary: Delete a user and their attendance records
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: User deleted successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResponse"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /attendance:
    post:
      operationId: markAttendance
      tags: [attendance]
      summary: Mark attendance for a user (auto check-in or check-out)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkAttendanceRequest"
      responses:
        "201":
          description: Attendance marked successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceRecord"
        "400":
          description: Bad request or user not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Already checked in and out today
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    get:
      operationId: getAttendance
      tags: [attendance]
      summary: Get all attendance records
      responses:
        "200":
          description: List of attendance records
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceListResponse"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    RegisterUserRequest:
      type: object
      properties:
        employeeId:
          type: string
          description: Unique employee ID
        name:
          type: string
          description: Full name of the user
        department:
          type: string
          description: Department of the user
        image:
          type: string
          description: Base64 encoded image data (PNG)
        faceDescriptor:
          type: array
          items:
            type: number
          description: 128-float face recognition descriptor from face-api.js
      required:
        - name
        - image

    RegisterUserResponse:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
        user:
          $ref: "#/components/schemas/User"
      required:
        - success
        - message

    User:
      type: object
      properties:
        id:
          type: integer
        employeeId:
          type: string
          nullable: true
        name:
          type: string
        department:
          type: string
          nullable: true
        imagePath:
          type: string
        hasFaceDescriptor:
          type: boolean
        workDays:
          type: integer
        earlyLeaveCount:
          type: integer
        sickLeaveDays:
          type: integer
        personalLeaveDays:
          type: integer
        absentDays:
          type: integer
        otHours:
          type: number
        overallStatus:
          type: string
        registeredAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - imagePath
        - hasFaceDescriptor
        - workDays
        - earlyLeaveCount
        - sickLeaveDays
        - personalLeaveDays
        - absentDays
        - otHours
        - overallStatus
        - registeredAt

    UserDescriptor:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        faceDescriptor:
          type: array
          items:
            type: number
      required:
        - id
        - name

    UserDescriptorsResponse:
      type: object
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/UserDescriptor"
      required:
        - users

    UsersListResponse:
      type: object
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/User"
      required:
        - users

    MarkAttendanceRequest:
      type: object
      properties:
        name:
          type: string
          description: Name of the user marking attendance
        type:
          type: string
          enum: [check_in, check_out]
          description: Force a specific attendance type. If omitted, auto-detected (even=check_in, odd=check_out)
      required:
        - name

    AttendanceRecord:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: integer
        userName:
          type: string
        date:
          type: string
          format: date
        type:
          type: string
          description: check_in or check_out
        timestamp:
          type: string
          format: date-time
      required:
        - id
        - userId
        - userName
        - date
        - type
        - timestamp

    AttendanceListResponse:
      type: object
      properties:
        records:
          type: array
          items:
            $ref: "#/components/schemas/AttendanceRecord"
      required:
        - records

    DeleteResponse:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
      required:
        - success
        - message

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
        error:
          type: string
      required:
        - success
        - error
